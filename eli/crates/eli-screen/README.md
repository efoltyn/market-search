# eli-screen (internal)

This crate is an internal workspace component of [`eli`](https://crates.io/crates/eli).

- Install the product with: `cargo install eli`
- Repository docs and usage: <https://github.com/efoltyn/eli>

`eli-screen` is kept publishable so `eli` can resolve workspace dependencies on crates.io.
